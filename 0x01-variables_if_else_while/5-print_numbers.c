#include <stdio.h>

/*
 * main - Entry point
 * Description: A C program that preints  prints all single digit numbers.
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int N = 0;

	while (N < 10)
{
	printf("%i", N);
	N++;
}
	printf("\n");
	return (0);
}
